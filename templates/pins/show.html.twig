{% extends 'base.html.twig' %}

{% block title %}{{pin.titre}}{% endblock %}

{% block body %}
        <article>
            <h1>{{pin.titre}}</h1>
            <p>{{pin.description}}</p>

        </article>
        <a href="{{path('app_home')}}">Go Back</a>
{% endblock %}